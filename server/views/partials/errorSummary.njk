{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}

{% if errors | length %}
  {{
    govukErrorSummary({
      titleText: __('errorSummary.title'),
      errorList: errors | errorSummaryList
    })
  }}
{% endif %}
