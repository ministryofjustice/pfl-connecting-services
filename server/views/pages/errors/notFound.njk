{% extends "partials/layout.njk" %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ __('errors.notFound.title') }}</h1>
      <p class="govuk-body">{{ __('errors.notFound.check') }}</p>
      <p class="govuk-body">{{ __('errors.notFound.homePage') | safe }}</p>
    </div>
  </div>
{% endblock %}
