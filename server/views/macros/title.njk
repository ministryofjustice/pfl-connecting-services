{% macro getTitle(id, errors) %}
  {% if errors | findError(id) %}
    {% set pageTitle = __("error") + ": " + __("pages." + id + ".title") %}
  {% else %}
    {% set pageTitle = __("pages." + id + ".title") %}
  {% endif %}

  {{ pageTitle }} – {{ __("serviceName") }} – GOV.UK
{% endmacro %}
